{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Publish Mikolayunas Website",
      "type": "shell",
      "command": "git add -A; git diff --cached --quiet; if ($LASTEXITCODE -eq 0) { Write-Host 'No changes to commit.'; exit 0 }; git commit -m 'Update site'; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; git push origin main",
      "options": {
        "cwd": "C:\\website\\mikolayunas",
        "shell": {
          "executable": "powershell.exe",
          "args": ["-NoProfile", "-ExecutionPolicy", "Bypass", "-Command"]
        }
      },
      "presentation": {
        "reveal": "always",
        "panel": "shared",
        "clear": false
      },
      "problemMatcher": []
    }
  ]
}